<div class="flex-centered">
  <% if current_user.id.to_s != params[:id] && action_name != 'profile' %>
    <%= button_to "Add Friend", invite_user_path(params[:id]), class: "is-button" %>
  <% end %>
  Friends:
  <%= render @user.friends %>
<div>
